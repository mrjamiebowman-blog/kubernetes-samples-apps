apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consumer-customers.fullname" . }}-test-connection"
  labels:
    {{- include "consumer-customers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "consumer-customers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
