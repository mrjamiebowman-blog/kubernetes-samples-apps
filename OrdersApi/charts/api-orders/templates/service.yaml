apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-orders.fullname" . }}
  labels:
    {{- include "api-orders.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-orders.selectorLabels" . | nindent 4 }}
