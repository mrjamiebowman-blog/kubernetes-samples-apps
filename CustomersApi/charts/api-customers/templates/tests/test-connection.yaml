apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-customers.fullname" . }}-test-connection"
  labels:
    {{- include "api-customers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-customers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
