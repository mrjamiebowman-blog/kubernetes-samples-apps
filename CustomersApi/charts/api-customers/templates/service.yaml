apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-customers.fullname" . }}
  labels:
    {{- include "api-customers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-customers.selectorLabels" . | nindent 4 }}
