apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-identityserver.fullname" . }}
  labels:
    {{- include "app-identityserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-identityserver.selectorLabels" . | nindent 4 }}
